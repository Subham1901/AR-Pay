export const USER_LOGIN = "USER_LOGIN";

export const loginUser = (type, payload) => {
  return { type, payload };
};

export const USER_SIGNUP = "USER_SIGNUP";

export const signUpUser = (type, payload) => {
  return { type, payload };
};

export const USER_LOGOUT = "USER_LOGOUT";
